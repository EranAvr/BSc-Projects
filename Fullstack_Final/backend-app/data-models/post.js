class PostModel {
    constructor(pid, title, text, bgColor='') {
        this.pid = pid;
        this.timeStamp = 0;
        this.title = title;
        this.text = text;
        this.bgColor = bgColor;
    }
}